import { StyleSheet } from 'react-native';

export const colors = {
  background: '#25292e',
  white: '#fff',
  offWhite: '#AFAFAF',
  black: '#000',
};

export const commonStyles = StyleSheet.create({
  textInput: {
    width: 250,
    height: 40,
    margin: 12,
    marginHorizontal: 20,
    color: '#fff',
    borderWidth: 3,
    borderColor: '#fff',
    padding: 10,
  },
});

